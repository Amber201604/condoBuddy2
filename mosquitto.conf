# CondoBuddy2 — Mosquitto MQTT broker configuration
# Dev-friendly defaults: anonymous access enabled. Lock this down for production.

persistence false
log_dest stdout

# Standard MQTT (used by the Core backend and IoT sensors)
listener 1883
protocol mqtt

# MQTT over WebSockets (for browser/PWA clients)
listener 9001
protocol websockets

allow_anonymous true
